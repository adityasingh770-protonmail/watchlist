[project]
name = "frame-api"
version = "0.1.0"
description = "API for the Frame watchlist"
requires-python = ">=3.14"
dependencies = [
  "fastapi>=0.115.0",
  "httpx>=0.28.0",
  "python-dotenv>=1.0.1",
  "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false
